/*
let config = {
    'baseUrl': location.pathname == '/console/' ? location.protocol+'//'+location.host: 'http://qa.bituquanguan.com:8000',
};
*/
let config = {
    'baseUrl': location.pathname == '/console/' ? location.protocol+'//'+location.host: 'http://127.0.0.1:8000',
};

export default config;
